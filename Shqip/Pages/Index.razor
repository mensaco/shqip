@page "/"

@inject NavigationManager Navi


@code{       

    private string Json { get; set; }

    protected override void OnInitialized()
    {
        //Json = Newtonsoft.Json.JsonConvert.SerializeObject(Templater.Scaffold(typeof(Model.Emer), "", "Rase"));
    }

}